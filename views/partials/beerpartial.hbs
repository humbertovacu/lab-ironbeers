 <div class = "product-beer">
    {{#each theBeers}}
        <img src="{{image_url}}" alt="beer image">
        <h1>{{name}}</h1>
        <h2>{{tagline}}</h2>
        <p>{{description}}</p>
    {{/each}}
</div>